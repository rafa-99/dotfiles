#!/bin/sh

case $DISTRO in
	gentoo)
		doas emerge -av --depclean
		;;
	arch)
		doas pacman -Rns $(pacman -Qtdq) && doas pacman -Scc
		;;
	parabola)
		doas pacman -Rns $(pacman -Qtdq) && doas pacman -Scc
		;;
esac
