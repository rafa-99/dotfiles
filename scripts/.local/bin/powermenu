#!/bin/sh

OPERATION=$(printf "Reboot\nShutdown\nSuspend" | dmenu -i -c -l 15 -p "Power Menu:")

case $OPERATION in
	Reboot)
		doas reboot
		;;
	Shutdown)
		doas poweroff
		;;
	Suspend)
		slock &
		loginctl suspend
esac
