#!/bin/sh

OPERATION=$(printf "Reboot\nShutdown\nSuspend" | dmenu -i -p "Power Menu:")

case $OPERATION in
	Reboot)
		sudo reboot
		;;
	Shutdown)
		sudo poweroff
		;;
	Suspend)
		slock &
		systemctl suspend
esac
