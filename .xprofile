#!/bin/sh

# Detecting Session and it's requirements
case "$SESSION" in
	bspwm)
		sxhkd &
		polybar laptop &
		;;
	dwm)
		dwmbar &
		;;
	xfce)
		;;
esac

# General Requirements
nitrogen --set-scaled "$WALLPAPER" &
picom &
setxkbmap pt &
numlockx on &
