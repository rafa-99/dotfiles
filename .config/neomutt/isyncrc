# Choose an IMAPAccount name
IMAPAccount SETACCOUNT

# Address to connect to
# IMAP Address
Host SETIMAPSERVER

# Email Account
User SETEMAIL
PassCmd "gpg -q --for-your-eyes-only --no-tty -d pwd.gpg | awk '{print $3}' | sed s/\"//g"

# Use SSL
SSLType IMAPS

# Certificate Location
CertificateFile /etc/ssl/cert.pem

# IMAP Configs
IMAPStore SETACCOUNT-remote
Account SETACCOUNT

# Local Repo Configs
MaildirStore SETACCOUNT-local
Subfolders Verbatim

# The trailing "/" is important
Path ~/Documents/Mail/SETACCOUNT/
Inbox ~/Documents/Mail/SETACCOUNT/SETINBOX

# Sync Configs
Channel SETACCOUNT
Master :SETACCOUNT-remote:
Slave :SETACCOUNT-local:
# Include everything
Patterns *

# Automatically create and delete missing mailboxes, both locally and on the server
Create Both
Expunge Both

# Save the synchronization state files in the relevant directory
SyncState *
